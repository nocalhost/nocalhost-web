import styled from 'styled-components';

export const WrapList = styled.div`
    margin-top: 16px;
    border-radius: 8px;
`;

export const FlexBox = styled.div`
    display: flex;
    align-items: center;
    flex-wrap: nowrap;
`;
